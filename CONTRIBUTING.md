1. Install the "Biome" plugin from IntelliJ plugin marketplace in order to have proper formatting and linting
2. To start frontend, go to respective folder, and run `npm i` and `npm dev`
3. To start backend, go to respective folder, and run `npm i` and `npm start`